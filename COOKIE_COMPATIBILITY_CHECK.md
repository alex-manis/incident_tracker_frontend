# Проверка совместимости cookies после изменений на бэкенде

## ✅ Изменения на бэкенде

**Было:**
```typescript
path: "/api/auth/refresh"  // Cookie доступна только для этого пути
```

**Стало:**
```typescript
path: "/"  // Cookie доступна для всех путей
```

## ✅ Проверка фронтенда

### 1. **Axios конфигурация** ✅
- ✅ `withCredentials: true` установлен глобально в axios instance
- ✅ Все запросы используют этот instance
- ✅ Refresh запрос явно указывает `withCredentials: true`

### 2. **CORS на бэкенде** ✅
- ✅ `credentials: true` в CORS настройках
- ✅ `origin: config.frontendUrl` настроен правильно

### 3. **Vite Proxy** ⚠️ ТРЕБУЕТ ВНИМАНИЯ
**Текущая конфигурация:**
```typescript
proxy: {
  '/api': {
    target: 'http://localhost:3001',
    changeOrigin: true,
  },
}
```

**Проблема:** Proxy может не передавать cookies правильно в development режиме.

**Решение:** Нужно добавить настройки для cookies:
```typescript
proxy: {
  '/api': {
    target: 'http://localhost:3001',
    changeOrigin: true,
    secure: false, // для http в dev
    cookieDomainRewrite: '',
    cookiePathRewrite: '/',
  },
}
```

### 4. **Логика работы с cookies** ✅
- ✅ Login устанавливает cookie через бэкенд
- ✅ Refresh использует cookie автоматически
- ✅ Logout очищает cookie правильно
- ✅ Все запросы отправляют cookies через `withCredentials: true`

## 🔍 Что проверить

1. ✅ Все запросы используют `api` instance с `withCredentials: true`
2. ⚠️ Vite proxy настроен для передачи cookies (нужно исправить)
3. ✅ CORS настроен правильно на бэкенде
4. ✅ Cookie path изменен на "/" - это правильно для работы со всеми путями

## 📋 Рекомендации

1. **Исправить Vite proxy** для правильной передачи cookies в dev режиме
2. **Протестировать** работу cookies в production (где нет proxy)
3. **Убедиться**, что cookie правильно устанавливается и отправляется

## ✅ Итог

Фронтенд в целом готов к работе с новыми настройками cookies, но нужно исправить Vite proxy для development режима.
